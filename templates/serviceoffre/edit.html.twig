{% extends 'base.html.twig' %}

{% block title %}Edit Service Offer{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Edit Service Offer</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary mt-3">Save</button>
            <a href="{{ path('app_serviceoffre_index') }}" class="btn btn-secondary mt-3">Cancel</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
