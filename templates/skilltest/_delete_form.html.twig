<form method="post" action="{{ path('app_skilltest_delete', {'id_test': skilltest.id_test}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skilltest.id_test) }}">
    <button class="btn">Delete</button>
</form>
