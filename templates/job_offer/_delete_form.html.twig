<form method="post" action="{{ path('app_job_offer_delete', {'idOffer': job_offer.idOffer}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_offer.idOffer) }}">
    <button class="btn">Delete</button>
</form>
