<form method="post" action="{{ path('app_application_service_delete', {'id_app': applicationservice.id_app}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ applicationservice.id_app) }}">
    <button class="btn">Delete</button>
</form>
