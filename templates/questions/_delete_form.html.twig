<form method="post" action="{{ path('app_questions_delete', {'id_question': question.id_question}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.id_question) }}">
    <button class="btn">Delete</button>
</form>
