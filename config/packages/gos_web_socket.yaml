gos_web_socket:
    server:
        port: 8080
        host: 127.0.0.1
    client:
        firewall: main
        session_handler: null
    shared_config: true

    pushers:
        wamp:
            enabled: true
            host: 127.0.0.1
            port: 8080
